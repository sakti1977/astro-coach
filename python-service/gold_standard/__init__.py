"""Independent gold-standard oracles and fixtures for the ephemeris engine.

These modules must not import `chart`, `dasha`, `yogas`, or `doshas` — the
app is the system under test; this package is the reference.
"""
